{{page-toolbar pageTitle="Button" isDemo=true}}

{{#doc-content}}
  {{! BEGIN-SNIPPET buttons.example }}
  <div class="layout-row">
    {{#o-button onClick=(action "flatButton")}}Button with action{{/o-button}}
    {{#o-button noink=true primary=true}}Primary (noink){{/o-button}}
    {{#o-button disabled=true}}disabled{{/o-button}}
    {{#o-button warn=true}}warn{{/o-button}}
    {{#o-button href="http://emberjs.com/images/tomsters/original.png" target="_blank"}}href link{{/o-button}}
  </div>
  <p>
    {{#o-button raised=true onClick=(action "raisedButton")}}Button with action{{/o-button}}
    {{#o-button raised=true primary=true}}Primary{{/o-button}}
    {{#o-button raised=true disabled=true}}disabled{{/o-button}}
    {{#o-button raised=true warn=true}}warn{{/o-button}}
  </p>
  {{#custom-button}}
  <p>
    {{#o-button raised=true onClick=(action "targetButton")}}Button with target{{/o-button}}
    {{#o-button raised=true primary=true onClick=(action "targetButton")}}Button with bubble{{/o-button}}
    {{#o-button raised=true primary=true onClick=(action "targetButton") bubbles=false}}Button no bubble{{/o-button}}
  </p>
  <p>
    {{#o-button raised=true fab=true}}Fab{{/o-button}}
    {{#o-button raised=true mini=true}}Mini{{/o-button}}
    {{#o-button iconButton=true}}{{o-icon "accessibility"}}{{/o-button}}
  </p>
  {{/custom-button}}
  <p>
    {{o-button raised=true label="Blockless version"}}
  </p>
  {{! END-SNIPPET }}
  <h3>Template</h3>
  {{code-snippet name="buttons.example.hbs"}}

  <h3>Controller</h3>
  {{code-snippet name="buttons.component.js"}}

  {{o-api
    (p-section
      (p-row "onClick" "closure" "Action sent when the button is clicked.")
      (p-row "type" "string" 'Sets the html5 `type` attribute. Defaults to `"button"`, except for `<a>` link buttons.')
      (p-row "bubbles" "boolean" "Determines whether the Ember click event handler bubbles. Default is `undefined`, which bubbles.")
      (p-row "disabled" "boolean" "Whether the button is displayed as disabled and does not accept clicks.")
    )
    (p-section
      "Link buttons"
      (p-row "href" "string" "Displays the button as an `<a>` link to the specified destination URL.")
      (p-row "target" "string" 'Sets the `<a>` link target attribute, such as `"_blank"`.')
    )
    (p-section
      "Appearance"
      (p-row "fab" "boolean" "Display as a Floating Action Button.")
      (p-row "iconButton" "boolean" "Set when the contents contains an icon and adjusts CSS appropriately.")
      (p-row "label" "string" "Set the content of the button when used as a blockless component.")
      (p-row "mini" "boolean" "Display as a mini-sized button. Implies `fab`, unless `fab` is explicity set falsy.")
      (p-row "noInk" "boolean" "Suppresses the ripple effect when clicked.")
      (p-row "primary" "boolean" "Display as the primary button, more prominent that other buttons.")
      (p-row "raised" "boolean" "Display button with a 3-D effect.")
    )
    "color"
  }}

{{/doc-content}}
